import React from 'react';
import ProjectList from '../ProjectList';
import Page from '../Layout/Page';

export const Dashboard = () => {

    return (
        <Page Component={ProjectList} />
    );
}